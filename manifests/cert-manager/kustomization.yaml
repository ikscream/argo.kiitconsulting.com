apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The cloudflare-api-token Secret (ns cert-manager) is created out-of-band from
# op://ai-skills/cloudflare-api/api_token — never in git.
resources:
  - clusterissuer.yaml
