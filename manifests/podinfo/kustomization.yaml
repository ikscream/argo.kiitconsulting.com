apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Namespace is set by the Argo CD Application's destination (demo) with
# CreateNamespace=true, so it is intentionally not pinned here.
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
