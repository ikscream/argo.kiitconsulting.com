apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The argocd-notifications-secret (key telegram-token) is created out-of-band
# from op://ai-skills/wq2n2roohzy3aqod22tecundri — never in git.
resources:
  - configmap.yaml
